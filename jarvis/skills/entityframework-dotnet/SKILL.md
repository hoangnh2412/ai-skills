---
name: entityframework-dotnet
description: Đã gộp vào multitenancy-dotnet (persistence mặc định EF). Dùng skill đó khi tích hợp EF Core / tenant DB / UoW.
metadata:
  audience: hoangnh
  workflow: github
---

# Đã chuyển → `multitenancy-dotnet`

Skill này **không còn nội dung**. Persistence Jarvis mặc định là EF và nằm trong [multitenancy-dotnet](../multitenancy-dotnet/SKILL.md).

```text
@.opencode/skills/multitenancy-dotnet/workflows/init.md
```

Patterns: `multitenancy-dotnet/patterns/` (single-db, separate-tenant-db, hybrid, custom-di).
