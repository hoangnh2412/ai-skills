# ADR Index — trạng thái & việc còn lại

Chỉ mục mọi quyết định định hướng của repo. **Đọc bảng này trước** khi hỏi "ADR nào đang mở / còn phải làm gì". Trạng thái đã **đối chiếu repo thật** (skills/, `rules.json`, router, templates), không chép lại header ADR.

**Tên file:** `ADR-NNN-yyyy-MM-dd-slug.md` — mã `ADR-NNN` **bất biến**, cấp theo thứ tự thời gian, **không** đổi khi trạng thái đổi. Trạng thái sống ở bảng này, không nằm trong tên file. Gọi nhau bằng mã: *"ADR-008 làm trước"*, *"Q2 của ADR-009 đã được ADR-014 trả lời"*.

**Trạng thái:** 🟡 **Doing** — đang làm dở, chưa xong 100% · ⚪ **Todo** — chưa làm gì · 🔴 **Pending** — tạm thời chưa làm (hoãn có chủ đích) · 🟢 **Done** — đã làm 100% · 🟣 **Cancel** — bỏ, không làm nữa (ghi rõ ADR nào thay).

Bảng xếp theo **thứ tự ưu tiên hành động**: Doing → Todo → Pending → Done → Cancel.

| Mã | Ngày | ADR | Trạng thái | Ghi chú — việc còn lại / khối chặn |
|---|---|---|---|---|
| **ADR-027** | 2026-08-28 | [Module `toolbox/` — công cụ làm ra chính Minipower](ADR-027-2026-08-28-module-toolbox-cong-cu-lam-ra-minipower.md) | 🟡 Doing | **Q1–Q3 §6 chủ repo duyệt hết 2026-08-28; thi hành xong 8/8 bước §7 cùng ngày.** Module thứ tư `toolbox/`: nhóm skill *làm ra chính minipower*, người dùng là maintainer repo này (lọc qua ADR-022 QĐ-7 câu 2), **không publish sang repo product** (QĐ-4). `fundamentals/template-skill.md` → `toolbox/skills/minipower-toolbox-skill-author/` và **tổng quát hoá** cho mọi module lá-rời, không còn bó .NET/Jarvis (QĐ-2) — lý do: khuôn viết skill là *tài liệu* thì chỉ có tác dụng khi có người nhớ mở, và lỗi đắt nhất khi viết lá-rời (`description` mơ hồ ⇒ skill không bao giờ được gọi) không test nào bắt được. `PACK.md` khai đủ trường, trường không áp dụng để rỗng kèm chú thích — một schema, không ngoại lệ ngầm (QĐ-3), kéo theo enum `repo:` có thêm `minipower` trong `contracts/pack-manifest.md`. Tên `toolbox` không tự nêu ranh giới ⇒ **phép thử chống-sọt-rác** viết vào hub: *"skill này có được cài vào workspace dự án khách hàng không? Có → không thuộc toolbox"* (QĐ-5). Template đặt đuôi `.tpl` vì `link:check` quét mọi `.md` (QĐ-6). Verify: **441/441 test** (+5 `toolbox-pack.test.js`, +4 `pack-manifest` nay phủ 4/4 module thay vì 2/4) · `gen:check` xanh · `link:check` 0 gãy mới · 0 link còn trỏ file đã xoá (chữ trong ADR-021/022 **giữ nguyên**, chỉ sửa href — QĐ-11 ADR-022). **Chỉ còn T1 smoke** (kích hoạt bằng `description` trong Cursor thật) — chủ repo tự chạy, xanh thì chuyển 🟢. Q1 kèm định hướng: `fundamentals/` còn lại là **nội dung cũ viết tạm**, chuẩn hoá dần thành skill (ngoài phạm vi ADR-027) |
| **ADR-028** | 2026-08-28 | [Hạ `fundamentals/` thành kho tạm + đổi tên `staging/`](ADR-028-2026-08-28-ha-fundamentals-thanh-kho-tam.md) | 🟡 Doing | **Q1–Q4 §6 chủ repo duyệt hết 2026-08-28; Đợt 0 thi hành xong cùng ngày — T1–T5 §8 xanh cả 5.** Đã làm: xoá `tutorial-index.md` + `tabler-uikit-skill.md` · **đổi tên `fundamentals/` → `staging/`** + sửa toàn bộ ref (README ×4 · AGENTS · toolbox ×2 · convention-dotnet · baseline · href ADR) · README/AGENTS khai `staging/` là **Kho tạm**, tầng nền chỉ còn `contracts/` + `ADRs/` · [ADR-021](ADR-021-2026-08-24-doi-ten-pack-jarvis-thanh-backend.md) nhận mục *Điều chỉnh 2026-08-28*. **Nợ link baseline 67 → 22** (kho 50 → 5); còn **13 file gốc** + `interview/`. **Đợt 1 xong 2026-08-28 qua [ADR-029](ADR-029-2026-08-28-skill-architecture-dotnet-thay-foundation-application.md)** — rút 3 hàng §5a cùng lúc: #3 `dotnet-clean-architecture.md` + #4 `dotnet-ddd.md` → `reference/` của skill `minipower-backend-architecture-dotnet` (mới), #5 `dotnet-structure.md` → gộp `scaffold/reference/solution-structure.md`. **Kho 13 → 10 file gốc.** Lưu ý: ADR-029 gộp 3 file trong **một** thay đổi, khác QĐ-4 ở đây (*"rút từng file một"*) — chủ repo chốt có ý thức tại ADR-029 Q17 vì 3 file cùng nói về kiến trúc/cấu trúc solution. *Còn lại: Đợt 1..n — rút từng file theo bảng §5a, thứ tự chưa chốt.* `fundamentals/` thôi là **"tầng nền"** ngang `contracts/` — hạ thành **kho tạm chỉ rút không nạp**, nội dung cũ viết tạm chờ chuẩn hoá thành skill/template (QĐ-1; định hướng chốt tại [ADR-027](ADR-027-2026-08-28-module-toolbox-cong-cu-lam-ra-minipower.md) §6 Q1). **Đổi tên `fundamentals/` → `staging/`** vì tên cũ (*"nền tảng"*) nói ngược hẳn vai thật; ADR này **điều chỉnh [ADR-021](ADR-021-2026-08-24-doi-ten-pack-jarvis-thanh-backend.md) QĐ-8** (chốt giữ tên) — lúc ADR-021 đổi tên phải soát 50 link để đổi lấy một chữ nên không đáng, sau khi xoá `tutorial-index.md` thì 44 trong số đó biến mất, cùng quyết định nhưng chi phí khác hẳn. Số liệu 2026-08-28: **50/67 nợ link gãy toàn repo (75%) nằm trong `fundamentals/`**, riêng `tutorial-index.md` **44** — mà chính file đó tự khai *"giữ làm tham chiếu lịch sử"* trong khi README trỏ nó làm ô *Bắt đầu*; 3 file tự xưng "Skill …" trỏ path không tồn tại; 2 cặp trùng vai (`incident-template` ↔ `TPL-incident-report`, `code_review` ↔ `review-dotnet`). **QĐ-2 lật ngược đề xuất ban đầu: KHÔNG đặt cổng cứng** — phương án *ratchet test* (hằng số trần số file, vượt là `npm test` đỏ) đã cân nhắc và **bỏ**: người duy nhất nạp vào kho là chính chủ repo, còn cái giá là một hằng số phải nhớ hạ mỗi lần rút. Hệ quả theo "cứng bằng máy, mềm bằng lời": luật này viết là **quy ước**, mọi nơi mô tả nó **không được dùng chữ "bắt buộc"**; dấu hiệu mở lại là số file **tăng**. Bảng §5a (QĐ-3 — giữ làm cam kết, thi hành sau) khai **đích cho cả 14 mục, không ô nào "chưa biết"**: 6 rút được ngay · 6 chờ ADR mở `autotest/` hoặc thi hành ADR-008 · `FundamentalClaudeCode.md` **giữ** làm tài liệu cá nhân (Q3) · `interview/` giữ tạm. Rút **từng file một, mỗi file một thay đổi Full** (QĐ-4). Không mở module mới trong ADR này (QĐ-6, giữ ADR-022 QĐ-7 luật 2) |
| **ADR-029** | 2026-08-28 | [Skill kiến trúc DDD thay `foundation` + `application`](ADR-029-2026-08-28-skill-architecture-dotnet-thay-foundation-application.md) | 🟡 Doing | **QĐ-1…13 duyệt hết 2026-08-28; thi hành xong 11/11 bước §7 cùng ngày.** Verify: **441/441 test** · `gen:check` xanh · `link:check` **0 gãy MỚI** (241 file · 1814 link · 27 nợ baseline) · grep tên cũ 0 hit ngoài `ADRs/` · `backend/skills/` còn **14** thư mục. **T7 hoá ra no-op** — QĐ-9 định gỡ href trong ADR-021 nhưng hai tên đã xoá chỉ nằm dưới dạng **code span** và trong khối code §3, chưa từng là link ⇒ file không đổi một byte. **Điều chỉnh 2026-08-29 — hai luật cắt ngang đi theo mọi skill lá** (chủ repo yêu cầu: *code C# ⇒ convention · dùng Jarvis ⇒ architecture*). Soát 14 lá: con trỏ `convention` chỉ có 8/14, `architecture` **3/14**. Nói thẳng giới hạn — minipower **không ép được agent tuân thủ**; thứ ép được là (a) **invariant repo**: mọi skill lá phải MANG cả hai con trỏ để skill nào kích hoạt thì luật cũng đi cùng, nay có **2 test canh** trong `backend-pack.test.js`; (b) **cổng ở dự án đích**: analyzer/`dotnet format` (convention) + `ArchitectureTests` R1–R7 (architecture) — đã dựng ở QĐ-5/QĐ-6. Nay **14/14 lá trỏ cả hai**; `convention` nhận bảng ranh giới hai câu hỏi; `review-dotnet` nhận mục *"Hai nguồn luật"* nêu rõ phần nào **máy đã gác** để khỏi nitpick tay; `observability` dùng câu riêng (output là dashboard/alert, không phải solution .NET) kèm luật *metric/enricher là adapter, không nhét vào Domain*. **Không** đụng 32 SKILL.md con trong `providers/`·`patterns/` — snippet luôn đi qua skill cha, thêm vào là 32 chỗ phải giữ đồng bộ. **T11 chứng minh cổng sống**: gỡ một link → `npm test` **ĐỎ đúng chỗ** kèm message, khôi phục → xanh. **443/443** (441 → 443) · `gen:check` xanh · `link:check` 0 gãy mới. **Còn lại T1–T3 (smoke trên solution .NET thật: scaffold → `dotnet test -c Debug` xanh · cố tình vi phạm phải thấy ĐỎ · thử kích hoạt skill bằng `description` trong Cursor) — chủ repo tự chạy, xanh thì chuyển 🟢.** QĐ-7 nhận phát biểu chuẩn của chủ repo: *architecture* = **"File nằm ở đâu? Reference thế nào?"**, *convention* = **"Code trong file viết thế nào?"**. Module `backend` **15 lá → 14**: xoá `minipower-backend-foundation-dotnet` (QĐ-1) + `minipower-backend-application-dotnet` (QĐ-2), mở `minipower-backend-architecture-dotnet` (QĐ-3). Bằng chứng §1 (đọc file thật 2026-08-28): `foundation/workflows/init.md` trùng nguyên văn `scaffold/templates/layers/HostLayerExtension.cs` · `appsettings-foundation.json` và `program-setup.cs` đều là **tập con** của bản scaffold (chỉ lệch `"Cors": {}`) · `application/workflows/init.md` trùng `ApplicationLayerExtension.cs` (3 dòng thật). Thứ duy nhất còn sống là `application/workflows/add.md` — nhưng `description` của nó lại mô tả việc *khởi tạo* chỉ chạy một lần, nên lá-rời không được gọi vào đúng lúc cần nhất (P2 — ADR-022 QĐ-5b). **Lý do mở skill kiến trúc chứ không chỉ thu hẹp `application`:** repo mới chỉ có skill cho **trục ngang** (12 lá Atomic Module), **trục dọc** — 5 layer + luật phụ thuộc một chiều của Jarvis — không ai giữ, mà một tính năng thật là **lát cắt dọc** chạm cả 5 layer chứ không phải một handler (QĐ-4). **QĐ-5 là lõi:** *"agent luôn tuân thủ kiến trúc"* **không viết bằng chữ** — markdown không FAIL được bằng máy (phép thử ADR-020 QĐ-3) ⇒ cổng cứng là **architecture test** trong `tests/{Product}.ArchitectureTests` (`Domain` không reference `Application`/`Infrastructure`/`Host`/EF · Controller không inject `DbContext`) — sai là `dotnet test` đỏ. Hai tầng cứng/mềm theo đúng khuôn `convention-dotnet` (QĐ-6); ranh giới với convention: **kiến trúc lo *chỗ đặt code + hướng phụ thuộc*, convention lo *cách viết dòng code*** — không chép lại quy ước nào (QĐ-7). Tri thức `foundation` **tán chứ không xoá**: 2 anti-pattern + note transitive `Jarvis.Mvc` → `review-dotnet`, `"Cors": {}` → scaffold appsettings (vá luôn chỗ lệch) (QĐ-8). ADR-021 **giữ nguyên chữ, chỉ gỡ href** (QĐ-9, biến thể ADR-022 QĐ-11 cho ca file biến mất). `backend-pack.test.js` ngưỡng `>= 14` **không nới**. **QĐ-10** — ADR này là đợt thi hành **[ADR-028](ADR-028-2026-08-28-ha-fundamentals-thanh-kho-tam.md) §5a hàng #3/#4**: rút `staging/dotnet-clean-architecture.md` + `staging/dotnet-ddd.md` thành `reference/` của skill mới (kho **13 → 11** file), kèm vá ref chết `architechture-dotnet.md` và chốt mâu thuẫn *interface repository ở Domain* theo README Jarvis. **QĐ-11 — chưa mở skill microservice**: lọc qua ADR-022 QĐ-7 câu (2) = không (vẫn .NET/Jarvis, vẫn backend dev); phần đặc thù Jarvis hỗ trợ hôm nay đã có lá riêng, phần thật sự riêng (event bus · outbox/inbox · integration events) README Jarvis khai **📋 roadmap chưa ship** ⇒ viết bây giờ là lý thuyết chung, trái *wrap-not-build*; ranh giới service trú tạm ở strategic DDD (bounded context); **dấu hiệu mở** = Jarvis chuyển event bus 📋→✅. **Q2 chốt tên `minipower-backend-architecture-dotnet`** (gạch nối — chủ repo gõ dấu chấm, là nhầm: regex `backend-pack.test.js` + ADR-022 QĐ-4 ép kebab). **§6a** bảng số liệu NuGet đọc 2026-08-28 cho Q3: `NetArchTest.Rules` 1.3.2 **2021** (chết) · fork `eNhancedEdition` 1.4.5 2025 (bus factor 1) · `TngTech.ArchUnitNET` **0.13.4 phát hành 2026-08-20** (TNG, nhịp đều) · tự viết 0-package — **đề xuất ArchUnitNET**, lý do là *vòng đời* chứ không phải cú pháp, giá phải trả là API 0.x. **§6b** cho Q4: đề xuất **mặc định có** (`5 project + 3 test`), lý do thực nghiệm *"architecture test thêm muộn là architecture test bị xoá"*. 9 bước §7 · 8 hàng T §8 (T2 = cố tình vi phạm phải thấy đỏ). **§6c — 11 điểm phải chốt trước khi viết luật máy**, sinh ra từ việc đối chiếu 2 file `staging/` với **scaffold thật**: 4 mâu thuẫn khiến *luật viết đúng chữ tài liệu thì solution vừa scaffold ra đã đỏ* — UoW interface docs nói Application nhưng `IAppUnitOfWork.cs` đặt ở **Domain** (Q7) · `Application.csproj` có `<FrameworkReference Microsoft.AspNetCore.App>` trong khi clean 3.1 cấm Application biết ASP.NET (Q8) · bảng package nói Domain *"không bắt buộc Jarvis"* nhưng template `using Jarvis.DDD.Domain.Repositories`, và `layer-csproj/` **thiếu hẳn `Domain.csproj.xml`** (Q9) · clean 3.1 cấm Host→Domain nhưng Jarvis bắt Host cài `Jarvis.DDD.Domain` cho `IWorkContext` (Q10). Kèm Q12 **báo lỗi lúc build hay test** (đề xuất cả hai: reference sai cấp project → `Directory.Build.targets` cho `dotnet build` đỏ ngay, luật cấp kiểu → `dotnet test`) · Q14 chỉ ép **assembly** không ép namespace (`ddd` 6.I có 2 kiểu + `solution-structure` thêm kiểu thứ 3) · Q15 **xác minh license AutoMapper** trước khi bê khuyến nghị sang skill mới · **QĐ-12 — nguồn chân lý kiến trúc = repo Jarvis nhánh `develop`**, không phải 2 file `staging/` (chủ repo chốt Q7–Q11: *"cứ tuân thủ kiến trúc Jarvis"*). Đọc csproj thật 2026-08-28 ⇒ **§5a**: chuỗi `Domain.Shared ← Domain ← Application.Contracts ← Application`, `ORM.EntityFramework → Domain`; `IUnitOfWork`/`IRepository` ở **`Domain/Repositories/`** · `ICommand`/`IQuery` ở **`Domain.Shared/Messaging/`** · handler/dispatcher ở **`Application.Contracts/`**. **Năm điều rút ra ghi đè `staging/`**: 3 điều cấm của `clean 3.1` **sai so với Jarvis** và bị loại trước khi kịp thành luật máy (`Newtonsoft.Json` — `Domain.Shared` dùng · `Microsoft.Extensions.*` — `DDD.Domain` dùng 3 gói `.Abstractions` · UoW-ở-Application — thực tế ở Domain, đóng Q7); 1 điều **đúng, giữ** (Domain ∌ EF Core → luật R2); 1 điều lộ ra **scaffold đang sai** — `templates/layer-csproj/Application.csproj.xml` thừa `<FrameworkReference Microsoft.AspNetCore.App>` trong khi `Jarvis.DDD.Application` không có gì, phải bỏ (đóng Q8) + bổ sung `Domain.csproj.xml` đang thiếu (Q9). **Q12 chốt: chỉ `dotnet test`** — bỏ hẳn cổng build, mọi luật về một cơ chế ArchUnitNET. **§6d** 7 luật R1–R7 viết lại theo Jarvis; R6 ghi rõ là **quy ước minipower, không rút được từ Jarvis** (`Sample/` là project đơn). **§6e** giải thích ép **assembly** thay vì namespace: `ddd` 6.I có 2 kiểu folder + `solution-structure` thêm kiểu thứ 3, ép namespace thì 2/3 kiểu đỏ oan dù kiến trúc đúng. **QĐ-13 (Q17 lật đề xuất): GỘP** `staging/dotnet-structure.md` 295 dòng vào scaffold cùng đợt ⇒ kho **13 → 10**, ADR thành ADR-đợt rộng, trái ADR-028 QĐ-4 — đánh đổi có ý thức. **Q15: giữ AutoMapper** dù đã xác minh 16.2.0 (2026-07-02) có ràng buộc license thương mại — nợ ghi lại. Q13/Q16 theo đề xuất; Q5/Q6 OK. **Trôi lệch ghi để không quên, ngoài phạm vi:** catalog scaffold ghi `Jarvis.EntityFramework` nhưng `develop` đã đổi tên `Jarvis.ORM.EntityFramework`, thêm `Jarvis.Multitenancy*` · `ORM.Dapper` · `OpenTelemetry.DDD` · `modules/` — cần đợt soát catalog riêng. 11 bước §7 · 9 hàng T §8 |
| **ADR-032** | 2026-09-03 | [Hình dạng gốc repo — `src/` cho module, `cli/` cho code cài đặt](ADR-032-2026-09-03-gop-module-vao-thu-muc-modules.md) | ⚪ Todo | **Q2–Q5 chủ repo chốt 2026-09-03; còn Q1 (duyệt) + Q6 (mở lại sau §5a).** Gốc repo trộn các loại thư mục khác bản chất và **phình tuyến tính theo số module** (8 → 10 khi `presales` + `evals` ra đời); thêm nữa **code cài đặt trú trong một module** (`sdlc/install/` cài cho cả repo) — đúng lỗi mà `sdlc/evals/` vừa sửa ở ADR-026 Q5, và ADR-031 sắp bồi CLI vào đó. QĐ-1 gom về **6 mục**: **`src/`** (5 module cài được) · **`cli/`** (code cài đặt) · tầng nền `contracts`/`ADRs` · kho `evals`/`staging`. **QĐ-2 LÀM NGAY, không chờ ADR-031** — căn cứ: chủ repo xác nhận **chưa có bản cài thật nào ngoài repo** (Q4), mà hook path trong `.claude/settings.json` là tuyệt đối ⇒ đây là cửa sổ rẻ nhất; chờ chỉ khiến `presales`+`evals` sinh ở gốc rồi dọn hai lần. QĐ-3 không symlink (không ai đang trỏ vào ⇒ không có gì để tương thích). **QĐ-4 tên skill `minipower-{module}-*` không đổi một ký tự** (ADR-022 QĐ-2/QĐ-4 nguyên vẹn), có hàng T4 canh bằng `git diff`. QĐ-5 di trú bằng script, `link:check`+`npm test` làm trọng tài. **§5a là phần đáng đọc nhất — dữ kiện lật một nửa đề xuất:** chủ repo muốn `cli/` chứa **cả `hooks/`**, nhưng `hooks.json` khai 6 shim theo **`${CLAUDE_PLUGIN_ROOT}/hooks/bin/*`** (plugin root = nơi có `.claude-plugin/` = `src/sdlc/`) ⇒ tách `hooks/` ra **làm chết kênh plugin** mà ADR-011 đã smoke PASS. Hai đường thoát E1 (dời `.claude-plugin/` lên gốc — plugin gói cả repo, chưa rõ loader thấy skill ở `src/sdlc/skills/` không) / E2 (symlink — mâu thuẫn QĐ-3) đều đắt hơn cái được. Thêm: `hooks/` **không phải code cài đặt** (`bin/*` chạy mỗi prompt, `lib/rules.json` là SSOT sinh bảng bên trong `src/sdlc/`), và số đo nói rõ — `cli/` chỉ động **6 link**, kéo `hooks/` theo là **48 link** nữa. ⇒ **QĐ-8: `cli/` chỉ chứa install; `hooks/` ở lại `src/sdlc/`** — Q6 để chủ repo lật nếu vẫn muốn gộp. Ghi nhận điểm lệch nhỏ chưa xử: `hooks/test/*-pack.test.js` thật ra là test của cả repo. Số đo 2026-09-03: **298 link** md (sdlc 257 · backend 27 · ops 12 · toolbox 11) / **44 file** · **17 hard-code** / 9 file · `pack-manifest.test.js:15` `MODULES = [...]`. Một đợt **7 bước** · **5 hàng T** §8 (T1 smoke `--plugin-dir src/sdlc` chủ repo tự chạy). Quan ngại đã nêu và chủ repo giữ nguyên: `src/` gợi codebase biên dịch trong khi repo cố ý không có build step |
| **ADR-026** | 2026-08-27 | [Phương pháp đánh giá chất lượng Minipower](ADR-026-2026-08-27-phuong-phap-danh-gia-chat-luong-minipower.md) | 🟡 Doing | **Q1–Q5 §6 chốt 2026-08-29; §7 viết lại thành 3 đợt + 3 sơ đồ.** **Q5 đổi vị trí: `evals/` đặt ở GỐC repo** (ngang `contracts/` · `ADRs/`), không phải `sdlc/evals/` — vì phạm vi ADR là *toàn bộ* minipower, đặt trong một module thì eval của `backend`/`ops` không có nhà; phân biệt bằng `evals/{module}/`. Kéo theo **Q2 chặt lại**: runner tự viết là chính thức, bỏ đường lai *bật-early-access-thì-đổi-runner* (`--eval-dir` đòi thư mục nằm dưới plugin). Khung 4 tầng: **T1** kỹ thuật (CI, đã có — duy trì) · **T2** nội dung skill (QĐ-2b **được duyệt và làm TRƯỚC**: WARN advisory SKILL > 150 dòng + "bắt buộc" không kèm check máy) · **T3** hành vi agent — **lỗ hổng lớn nhất**: kịch bản golden tại `sdlc/evals/`, đợt đầu **chỉ readiness-gate**, **6 kịch bản** ma trận mode **thưa** ≈ 9–10 case (bỏ "6–8 × 3 mode = 24 case" — chi phí giả) · **T4** outcome dự án đích từ artifact có sẵn (`trace:check --stats` · gate precision · tuổi nợ doc · rework sau baseline). **Vòng thực nghiệm §6a (2026-08-29):** `claude plugin eval` có đủ tính năng nhưng `init` trả *"early access"*; `claude -p --output-format json` chạy được, $0.02/call rỗng, tự đo được chi phí; `--plugin-dir sdlc` cho C3 thoả tự nhiên (nạp plugin thật). Giữ **schema** `plugin eval` (`case.yaml` + `prompt.md` + `graders/*.md`) vì schema tốt, không phải để đổi runner. **§7b là điểm cụ thể hoá lớn nhất:** áp chính "cứng bằng máy, mềm bằng lời" lên bản thân eval — **6/7 grader chấm bằng máy** trên transcript (hỏi-một-lượt · recall bẫy · không sinh artifact trước verdict · ghi nợ đúng file theo mode · dạng verdict · không đoán module), chỉ *chất lượng câu hỏi* mới cần judge + người soát 20%. Điểm **vẫn advisory** (giữ ADR-020 QĐ-11). **Đợt 1 có điểm dừng tự nguyện:** variance quá lớn ⇒ hạ QĐ-3 xuống checklist người tự soát, **không** tăng số run để mua ổn định. *Còn lại:* **Đợt 0** (`trace:check --stats` · 2 check QĐ-2b) → **Đợt 1** spike 1 kịch bản → **Đợt 2** 9–10 case + baseline. **3 điểm mở §6b phải đóng trước Đợt 1:** M1 bật early-access? · M2 chữ "bắt buộc" trong repo — **sửa chữ** hay whitelist (không đóng thì T4 *0 false-positive* bất khả) · M3 `plugin.json` khai `name: "minipower"` chứ không phải `minipower-sdlc` |
| **ADR-020** | 2026-08-20 | [3 chế độ dự án, điều kiện cứng bằng hook](ADR-020-2026-08-20-minipower-3-che-do-du-an-gate-bang-hook.md) | ⚪ Todo | **ADR định hướng hiện hành** — giữ kiến trúc pipeline + **một cấu trúc folder duy nhất cho cả 3 mode** (mode chỉ đổi *nội dung được điền*, không cắt folder — tương thích ngược, không phải di trú khi lên `standard`); thêm `project_mode` (mvp · standard · maintain) + `approval_source` (local · openproject · gitlab) vào `rules.json` + `profile.json` v2. Nguyên tắc **"cứng bằng máy, mềm bằng lời"**: điều kiện cứng là hook/CI (3 hook mới `prereq-gate`/`dec-gate`/`baseline-guard`, bỏ `permissions.deny` tĩnh), **test viết trước hook** (QĐ-10). Phê duyệt: local hôm nay → MCP ngày mai bằng back-ref, đổi một trường (QĐ-9). Skill mới `as-built` — người trigger, **wrap codegraph** (optional). `memory/` **không** cấu trúc lại (§3c). Huỷ ADR-019. **12 việc** ở §8. **§9 đã đóng 2026-08-21** — Q1–Q6 chốt hết ⇒ **QĐ-11** bỏ hẳn `dec-gate` (chữ ký thôi làm điều kiện máy kiểm; người ra lệnh, hệ cảnh báo; 3 hook mới → **2**) + **QĐ-12** `approval_source` tách `{docs,tasks,code}` + **QĐ-13** `prereq-gate` kiểm tiền đề **theo module** (module chạy lệch nhịp là bình thường) + **QĐ-14** `fan-out` = **pipeline theo module**, bỏ barrier "mọi module xong mới đi tiếp" ⇒ thêm việc **#7b** viết lại `fan-out/SKILL.md` + sửa `parallel-work.md` dòng 7. **Q1–Q9 chốt hết** (Q9 đóng tại việc #2). **Việc #1 🟢** (`project_modes` · `doc_scope` · `profile` v2) · **#2 🟢** (test đỏ) · **#3 🟢** (hook `prereq-gate` + `baseline-guard` + wiring 5→6 — **337/337 test**, `gen:check` xanh; cả 7 shim đều có integration test). **#4 🟢** (bỏ `permissions.deny` tĩnh, đồng bộ 3 kênh cài đặt + test parity; sửa bug installer chết trên Windows — **344/344 test**). **#5 🟢** (router: bảng mode generated, init 7 câu, init-vào-repo-có-sẵn — **344/344 test**). **#6 🟢** (skeleton: `assets/archive/` + `memory/doc-debt.md` + README folder rỗng — **357/357 test**). **#7 · #7b · #8 · #9 · #10 · #11 · #12 🟢** — **cả 13/13 việc §8 xong**: skill gate per-mode · `fan-out` = pipeline theo module (+ dọn 5 chỗ còn hứa cổng chặn) · templates 2 mức「lõi ▸ full」· skill `as-built` + `incident/` · `change-control` luồng chuyển mode + format `doc-debt` · **`trace:check` + CI Gitlab** · AGENTS.md/COORDINATION/README. **407/407 test**, `gen:check` xanh. *Còn lại: đường codegraph của `as-built` chưa xác minh (optional, chưa cài).* Chuyển 🟢 khi chủ repo xác nhận |
| **ADR-025** | 2026-08-26 | [Hỗ trợ Codex CLI — kênh cài thứ tư, SSOT không đổi](ADR-025-2026-08-26-ho-tro-codex-kenh-cai-thu-tu.md) | ⚪ Todo | **Chặn — chờ Confirm §6 (Q1–Q3).** Codex là **adapter thứ tư** trong `sdlc/install/` — SSOT giữ nguyên (`hooks/lib` · `rules.json` · `agents/*.md` · skill), client chỉ là glue mỏng (QĐ-1). Hooks: fragment khai đủ 6 shim dùng chung, schema lệch → shim chuyển đổi ở tầng `bin/`, lib không đổi (QĐ-2). Skills: symlink chuẩn `.agents/skills/` — không sửa SKILL.md nào (QĐ-3). Rules: đi qua AGENTS.md dự án đích (QĐ-4). **Vòng thực nghiệm N1–N4 trên Codex thật trước khi wire** (QĐ-5, khuôn ADR-012 vòng V). Parity mở rộng 3→4 kênh có test canh (QĐ-6) |
| **ADR-008** | 2026-07-25 | [Proposal Suite (GPKT · Báo giá · Timeline)](ADR-008-2026-07-25-minipower-proposal-suite.md) | ⚪ Todo | **Làm được ngay — hướng đã chốt, không câu hỏi chặn.** Repo chưa có gì: không `skills/proposal-*`, không `catalog-default.json`/`quotation-calc.js`; router + `rules.json` 0 hit "proposal". Lộ trình §6: **R2** catalog + `quotation-calc.js` + test (ROI cao nhất, làm trước) → **R3** skill quotation → **R4** `milestones[]` + `timeline-to-mermaid.js` (song song R2) → **R5** TPL GPKT + `gpkt-assemble.js` → **R6** wire router + `prereq_by_intent` (đụng `rules.json` ⇒ `gen → test → gen:check`) |
| **ADR-012** | 2026-07-26 | [Cursor Plugin](ADR-012-2026-07-26-minipower-cursor-plugin.md) | ⚪ Todo | **Mở lại 2026-08-25** (§7) — docs Cursor đã có local-dev install (`~/.cursor/plugins/local/` + symlink) ⇒ làm **plugin đầy đủ skill + rule + hook**, gốc plugin `sdlc/`, manifest `.cursor-plugin/`. Thứ tự: **vòng V** thực nghiệm đóng M2 (CWD hook — chặn W2) / M4 (namespace) / M5 (`agents: []`) trên Cursor thật → **vòng W** manifest + `hooks/hooks.cursor.json` sinh-từ-SSOT + parity 3 kênh + doc "chọn một kênh". V1 xấu → rơi về skills+rules, hook đi adapter cũ |
| **ADR-014** | 2026-07-28 | [Spine "wrap-not-build"](ADR-014-2026-07-28-minipower-spine-tong-hop-wrap-not-build.md) | ⚪ Todo | Chưa có `constitution.md`, chưa sharded step-file. **Chặn — cần người quyết:** Q1 vai wedge đợt đầu · **Q2 thứ tự borrow S1→B1→S2→O1→P2→B2** · Q3–Q6 |
| **ADR-016** | 2026-08-02 | [Discovery — tóm tắt tài liệu nguồn lớn](ADR-016-2026-08-02-minipower-discovery-tom-tat-tai-lieu-lon.md) | ⚪ Todo | `discovery/SKILL.md` chưa có Bước 0 Ingest. **Chặn — cần người quyết:** Q1–Q8 (chunk boundary/size, ngưỡng bật fan-out, có tạo skill `source-ingest` không) |
| **ADR-030** | 2026-08-29 | [Mở module `presales/` — ước lượng & báo giá, giữ nguyên `discovery`](ADR-030-2026-08-29-mo-module-presales-skill-uoc-luong-ulnl.md) | ⚪ Todo | **Chờ chủ repo duyệt Q1–Q9 §6** (tên module + 2 skill · `roles`/`owner` · input tối thiểu H0 · tín hiệu `risk()` · ±10% FAIL cứng hay cảnh báo · đơn giá theo vai trò · `mh_per_md` · hoãn `survey`). **Bản 3.** Bản 1 đặt skill trong `sdlc/skills/estimation/` — bị ba luận điểm bác (§1.2): DOC-14 khác họ phương pháp (WBS/Story Point vs mã loại catalog) · DOC-14 không có chi phí · **lúc báo giá chưa thể có DOC-14**, và buffer rủi ro không thuộc DOC nào. Bản 2 mở module nhưng **vẽ ngược boundary** và bỏ sót nguy cơ nhân đôi khảo sát. **Bản 3 chạy đủ cả hai câu [ADR-022](ADR-022-2026-08-24-minipower-nen-tang-cong-cu-ai-toan-cong-ty.md) QĐ-7** (§1.3): câu 1 trả lời *có* cho phần khảo sát ⇒ **`sdlc/skills/discovery/` giữ nguyên, không sửa một chữ** (QĐ-3, canh bằng T13); presales **dùng** discovery chứ không **sở hữu**; phần định giá không thuộc pipeline nào ⇒ mở **module thứ năm `presales/`**. Boundary **H0 = `Discovery (sơ bộ) → Estimation`** (producer owner là BA, không phải presales — bản 2 vẽ ngược); discovery **vắt ngang vạch ký**, tiếp tục sau khi ký chứ không làm lại. Mở với **2 skill thật**: `minipower-presales-estimation-ulnl` (ULNL §6–8, dừng ở Σ MH) + `minipower-presales-quotation` (§10–11, §14 — MD·buffer·ROM·tiền, **đơn giá một nhà duy nhất**); `estimate-vX.Y.json` là **hợp đồng có test canh** (QĐ-8). `survey`/`proposal`/slide = **tên trong README**, không thư mục (QĐ-2). Ranh giới: LLM **chỉ điền số đếm được có trích nguồn** (QĐ-12), chọn mã là bảng quyết định trong code (QĐ-11), buffer rủi ro là bậc dữ liệu (QĐ-14), anti-pattern §13 thành `validate()` (QĐ-15), **không-LLM là mặc định** khi đầu vào đã có số (QĐ-13). Kéo theo `contracts/` ×2 (H0 + enum `stage`). **[ADR-008](ADR-008-2026-07-25-minipower-proposal-suite.md):** R2+R3 chuyển sang đây, nhà mới `presales/`; ADR-008 **giữ ⚪ Todo** cho R4 timeline + R5 GPKT, không rewrite. `UCP.md` (#10) → lá anh em `-estimation-ucp`, **không** phải dựng lại lớp tiền (QĐ-18). T1–T16 §8 đỏ cả mười sáu |
| **ADR-031** | 2026-09-01 | [CLI `install`/`init` — script thi hành, LLM chỉ hỏi](ADR-031-2026-09-01-cli-install-init-thay-llm-thi-hanh.md) | ⚪ Todo | **Chờ chủ repo duyệt Q1–Q6 §6** (vị trí CLI O2 hay O3 · install có symlink skill không · `init --interactive` làm ngay hay để sau · fragment sinh-từ-SSOT trong đợt này hay tách · hai đợt liền hay dừng nghiệm thu giữa chừng). Áp **ADR-020 QĐ-3** "cứng bằng máy, mềm bằng lời" vào bề mặt cuối cùng chưa được chạm: **cài đặt + khởi tạo**. Hiện trạng lệch: 3 kênh cài nhưng **chỉ Claude có script** (`install/claude/install.mjs`), Cursor/OpenCode vẫn merge JSON + symlink tay; init dự án **100% do LLM** thi hành (8 bước `sdlc/SKILL.md`) và "Exit init" chỉ là checklist bằng chữ — không gì FAIL được bằng máy. **11 QĐ**: một CLI Node duy nhất `sdlc/install/minipower.mjs`, hai lệnh con **`install`** (wire client) và **`init`** (dựng dự án) — tách sạch như CodeGraph (QĐ-1) · **ranh giới nền QĐ-2: LLM hỏi/giải thích/gợi ý, script `mkdir`/`cp`/ghi JSON/verify — không LLM nào tự tay dựng cấu trúc nữa** · auto-detect client theo dấu vết thư mục (QĐ-3) · `--with` đọc `*/PACK.md` — **đóng ADR-022 QĐ-6**, `PACK.md` có consumer thật đầu tiên (QĐ-4) · **kênh = dữ liệu `channels.json`, không phải code** ⇒ ADR-025 rút gọn còn vòng thực nghiệm N (QĐ-5) · idempotent + `.bak` + verify cho cả 4 kênh (QĐ-6) · `init --answers`/`--interactive`, thiếu trường thì FAIL, **không tự điền** (QĐ-7) · **"Exit init" thành exit code** `init --check` (QĐ-8) · router 8 bước → 3 bước (QĐ-9) · **không tự động hoá phần phán đoán** — CLI không chọn `project_mode`, không sửa file sẵn có (QĐ-10) · chưa làm `npx`/packaging (QĐ-11). **Số điều kiện cứng vẫn là 7** — `init --check` là verifier gọi theo yêu cầu, không phải hook (C5). **Hai đợt §7:** Đợt A `install` (A1–A5) · Đợt B `init` (B1–B5). **8 hàng T §8 đều 🔴** |
| **ADR-011** | 2026-07-26 | [Plugin Claude Code (`minipower-dev`)](ADR-011-2026-07-26-minipower-claude-code-plugin.md) | 🟢 Done | **Chủ repo smoke PASS 2026-08-25:** `claude --plugin-dir D:/.../ai-skills/sdlc` từ folder sạch (`D:\Working\test-plugin`) — plugin nạp, skill namespaced hiển thị (`/minipower:discovery`, `/minipower:as-built`, …) đúng §5. Trước đó: 223 test, `gen:check` xanh. Không còn việc |
| **ADR-024** | 2026-08-25 | [Hub `backend/` — nguồn publish dời về repo minipower](ADR-024-2026-08-25-backend-hub-publish-nguon-minipower.md) | 🟢 Done | **Chủ repo xác nhận 2026-08-25.** Duyệt 5 QĐ + thi hành cùng ngày — verify 3/3 PASS (grep tàn dư = 0 · 432 test · link:check 0 MỚI). Không còn việc. Đã sửa mâu thuẫn nội bộ hub backend: mục Publish còn nguyên thời skill sống trong repo Jarvis — *"source of truth = `.opencode/` trong repo Jarvis (repo này)"* đá thẳng dòng 7 *"Repo `minipower` là source of truth"*. QĐ-1 nguồn = `minipower/backend/skills/` (framework Jarvis vẫn là repo ngoài — chỉ skill sống ở minipower); QĐ-2 giữ hình cơ chế submodule/symlink/rsync, đổi nguồn (`vendor/minipower`, rsync `backend/skills/` → `.opencode/skills/`); QĐ-3 `JARVIS_SKILLS_REF` → **`MINIPOWER_SKILLS_REF`** + tag `minipower-skills-vX.Y.Z` (⚠️ breaking nếu có pipeline consumer thật — hiện nội bộ, đổi sớm rẻ); QĐ-4 hub theo khuôn module (tiêu đề "Code backend .NET (backend)", đoạn lá-rời tự kích hoạt, câu H4 nhận DOC-08/11/12 khớp PACK.md); QĐ-5 không đổi phần cài + bảng skill (đợt ADR-023 sở hữu). Bảng sửa ~15 dòng ở §1; verify grep + test + link-check |
| **ADR-023** | 2026-08-25 | [Tách module `ops/` từ backend troubleshooting](ADR-023-2026-08-25-tach-module-ops-tu-backend-troubleshooting.md) | 🟢 Done | **Chủ repo xác nhận 2026-08-25.** Duyệt cả 5 QĐ, đợt §2 thi hành 6/6 bước cùng ngày — verify: 432 test xanh (+5 `ops-pack.test.js`) · `gen:check` · `link:check` 0 gãy mới · grep tên cũ sạch. Không còn việc. Tạo module `ops/` — vận hành hạ tầng, người dùng DevOps (lọc qua ADR-022 QĐ-7 câu 2); tương lai cài server/CI-CD cũng vào đây (QĐ-1). Chuyển `minipower-backend-troubleshooting-dotnet` → `ops/skills/minipower-ops-metrics` — bản chất là tầng thu thập Grafana/Prometheus, không gắn .NET, bỏ hậu tố `-dotnet` (QĐ-2, khuôn ADR-021 "đổi tên không đổi nội dung"). Skill case viết dần, không folder rỗng: `memory-leak-dotnet` · `deadlock-dotnet` · `log-flow` (QĐ-3). **Vai gắn bằng metadata (`roles:` PACK.md), không vào tên skill** (QĐ-4). **Skill nội bộ router-gộp `sdlc/` mang tên trơn ≡ thư mục, không tiền tố** — đã thi hành 2026-08-25: bỏ `ba-*` khỏi 6 skill phase, suite 427 xanh (QĐ-5) |
| **ADR-022** | 2026-08-24 | [Minipower — nền tảng công cụ AI toàn công ty](ADR-022-2026-08-24-minipower-nen-tang-cong-cu-ai-toan-cong-ty.md) | 🟢 Done | **Chủ repo xác nhận 2026-08-25.** Đợt §6 thi hành 8/8 bước, verify 5/5 PASS (đối chiếu baseline gốc 0 gãy ngoài nợ cũ · grep path sạch · test + parity · gen:check · nội dung bất biến) + 14 test canh invariant (suite 427). **Việc sau-đợt tách riêng, chưa mở:** `install.mjs --with` (QĐ-6) · ánh xạ MCP tên (QĐ-9) · OpenProject cho sdlc · nợ 50 link `.opencode` cũ trong fundamentals. — ADR chủ của đợt tái cấu trúc. Định vị: minipower = **Role Intelligence Layer** của Corporate AI Platform — sở hữu KHUÔN (quy trình/template/trace), không sở hữu ĐẤT SÉT (knowledge→Outline · code→GitLab/CodeGraph · task→OpenProject), không phải agent runtime (QĐ-1). **Hệ tên hai tầng** (QĐ-2): `minipower` = thương hiệu (repo·tiền tố·plugin); folder module = chức năng một-từ. **`minipower/` → `sdlc/`**, router đăng ký `minipower-sdlc` (QĐ-3). Tiền tố toàn cục `minipower-{module}-{capability}[-{stack}]` — gõ "minipower" là toàn bộ skill hiện ra (QĐ-4). `sdlc` = router-gộp, module kỹ thuật = lá-rời, chọn theo "từ vựng kích hoạt"; skill module cắm **cross-phase**, không thêm phase vào `rules.json` (QĐ-5). Cài theo module `--with` (QĐ-6). Quy tắc 3 câu hỏi phân loại việc mới + 2 luật: tên theo việc · chưa có skill thật chưa tạo folder (QĐ-7). **PACK.md manifest** máy-đọc = hạt giống Skill Registry (QĐ-8). **MCP bằng con trỏ tên** — gateway đến chỉ đổi ánh xạ (QĐ-9). **Tách COORDINATION.md → `contracts/` ×5** (trace-spine · handoff · lingua-franca · cross-repo-bridge · pack-manifest + README index), index ở AGENTS.md §Tham chiếu, mỗi file tự khai trạng thái, kèm sửa test `#12` (QĐ-10). ADR cũ: chữ giữ, **href sửa máy móc** (QĐ-11). **Không xây Control Plane** — 8 capability nâng cao là hạ tầng ngoài, repo tương thích bằng dữ liệu (QĐ-12). Thi hành **một đợt** §6 (8 bước, gồm cả ADR-021); verify: link-check + grep + gen/test/gen:check + parity + diff-nội-dung |
| **ADR-021** | 2026-08-24 | [Đổi tên pack `jarvis` → `backend`](ADR-021-2026-08-24-doi-ten-pack-jarvis-thanh-backend.md) | 🟢 Done | **Chủ repo xác nhận 2026-08-25. ⚠️ QĐ-8 và QĐ-6 đã bị [ADR-028](ADR-028-2026-08-28-ha-fundamentals-thanh-kho-tam.md) điều chỉnh 2026-08-28** (`fundamentals/` không còn giữ tên — đổi thành `staging/`; `tabler-uikit-skill.md` xoá thay vì chờ `frontend/`) — xem §9 trong file; phần còn lại giữ nguyên. Thi hành trong đợt ADR-022 — 15/15 skill đổi tên, `name:` khớp lá, nội dung bất biến (kiểm 5 PASS), invariant có test canh. Q1–Q4 đóng hết. Không còn việc. Tách "Jarvis = framework .NET" (giữ tuyệt đối: package, API, repo ngoài, `JARVIS_SKILLS_REF`) khỏi "jarvis = pack trong repo" (→ `backend/`). 15 skill lá `minipower-backend-*-dotnet` (dài nhất 40 ký tự; provider 47 — dưới ngưỡng 64); `code-review` → `minipower-backend-review-dotnet` (Q1) · `jarvis-dotnet` → `minipower-backend-scaffold-dotnet` (Q2 — tên theo việc, không theo framework) · pack manifest §5.3/§5.4 → `pack: backend`/`frontend` (Q3 — sau tách nằm ở `contracts/pack-manifest.md`) · script dò link **giữ lại** `hooks/bin/link-check.js` + `npm run link:check` (Q4). **Chỉ đổi tên, không đổi nội dung** (QĐ-5 — diff SKILL.md rỗng trừ `name:`). `frontend/` chưa tạo (QĐ-6). `rules.json` 0 hit "jarvis" ⇒ `npm test` không chứng minh gì cho refactor này — verify bằng link-check + grep (§6). Thi hành lồng trong đợt chung ADR-022 §6 |
| **ADR-010** | 2026-07-25 | [Checkpoint tạm dừng gated-fanout](ADR-010-2026-07-25-tam-dung-gated-fanout-checkpoint.md) | 🟢 Done | Checkpoint đã ghi xong; **đã xác minh 2026-08-20:** branch `feature/minipower-pm-project` đã merge `main` (0 commit ahead) → mục "chưa merge" hết hiệu lực. Đọc trước khi resume ADR-003 |
| **ADR-001** | 2026-07-17 | [Đánh giá Minipower & chiến lược phát triển](ADR-001-2026-07-17-danh-gia-minipower-va-chien-luoc-phat-trien.md) | 🟢 Done | P0–P4 + R1–R6 xong, đã merge main. Q4/Q5 chốt. Không còn việc |
| **ADR-002** | 2026-07-20 | [Định hướng — AI hỗ trợ ra quyết định](ADR-002-2026-07-20-dinh-huong-minipower-ai-ho-tro-ra-quyet-dinh.md) | 🟢 Done | N1–N6 đã triển khai. §0 bị **ADR-003** supersede; §1–§6 vẫn còn giá trị tham chiếu |
| **ADR-004** | 2026-07-25 | [Proposal skills — chỉ mục cha](ADR-004-2026-07-25-minipower-proposal-skills.md) | 🟢 Done | Khép hồ sơ: đã gộp vào **ADR-008**, không cập nhật tiếp. *Việc thực thi proposal theo dõi ở ADR-008 (Todo), không phải ở đây* |
| **ADR-005** | 2026-07-25 | [`proposal-quotation`](ADR-005-2026-07-25-proposal-quotation.md) | 🟢 Done | Khép hồ sơ: gộp vào **ADR-008 §3**. Giữ schema JSON §4 để tra cứu |
| **ADR-006** | 2026-07-25 | [`proposal-technical`](ADR-006-2026-07-25-proposal-technical.md) | 🟢 Done | Khép hồ sơ: gộp vào **ADR-008 §4** |
| **ADR-007** | 2026-07-25 | [`proposal-timeline`](ADR-007-2026-07-25-proposal-timeline.md) | 🟢 Done | Khép hồ sơ: gộp vào **ADR-008 §5** |

| **ADR-019** | 2026-08-20 | [Harness đa vai, không gate nội bộ](ADR-019-2026-08-20-minipower-harness-khong-gate.md) | 🟣 Cancel | Huỷ 2026-08-20 (cùng ngày viết) — thay bởi **ADR-020**: "không block bước nào" bỏ luôn khả năng gate cứng cho dự án chuẩn chỉnh; ADR-020 giữ kiến trúc + tham-số-hoá gate theo 3 chế độ dự án. Kế thừa: QĐ-5 advisory (→ hành vi mode mvp/maintain) · QĐ-6 `trace:check` CI · §3 ánh xạ vai→công cụ (Gitlab) · §2 phân loại 6 loại gate |
| **ADR-003** | 2026-07-20 | [Gated Fan-out Execution](ADR-003-2026-07-20-minipower-gated-fanout-execution.md) | 🟣 Cancel | Huỷ 2026-08-20 — lấy "gated" làm lõi, trái **ADR-019**. Kế thừa: cơ chế fan-out per-module (skill `fan-out` vẫn dùng, bỏ điều kiện DEC cổng trước), thứ tự requirements, DOC-19 |
| **ADR-013** | 2026-07-26 | [Senior/Junior execution](ADR-013-2026-07-26-minipower-senior-junior-execution.md) | 🟣 Cancel | Huỷ 2026-08-20 — lõi là QC loop **chặn** ≤3 + cổng nghiệm thu. Kế thừa: hợp đồng 7 trường + governance test-case như **khuyến nghị** |
| **ADR-017** | 2026-08-20 | [7 vai + toolchain (Github)](ADR-017-2026-08-20-minipower-toolchain-openproject-github-outline-slack.md) | 🟣 Cancel | Huỷ cùng ngày viết — sai công cụ (Github → **Gitlab**) + vẫn giữ khung gate. Kế thừa: ánh xạ vai→công cụ, L1/L2/L3 |
| **ADR-018** | 2026-08-20 | [Phê duyệt OpenProject + Outline](ADR-018-2026-08-20-minipower-phe-duyet-openproject-publish-outline.md) | 🟣 Cancel | Huỷ cùng ngày viết — viết theo kiểu "chưa duyệt = không qua cổng". Kế thừa: mô hình 3 mặt phẳng + publish Outline + back-ref DEC |
| **ADR-009** | 2026-07-25 | [Orchestrator analysis](ADR-009-2026-07-25-minipower-orchestrator-analysis.md) | 🟣 Cancel | Huỷ 2026-08-20 — câu hỏi chặn đã được **ADR-017** trả lời (Q2 = wrap MCP 4 công cụ · Q3 = cả 7 vai · Q6 = bỏ Calendar/Zoom). Phân loại **L1/L2/L3** và ranh giới "không agent-tự-bàn-giao" kế thừa vào ADR-017 |
| **ADR-015** | 2026-07-29 | [Phê duyệt Jira/Lark + Outline](ADR-015-2026-07-29-minipower-phe-duyet-jira-lark-publish-outline.md) | 🟣 Cancel | Huỷ 2026-08-20 — Jira/Lark rời lộ trình (ADR-017 QĐ-3), Q1 "Jira hay Lark" mất nghĩa. Mô hình 3 mặt phẳng viết lại tại **ADR-018** |

*Cập nhật lần cuối: 2026-09-03 · 32 ADR · mã kế tiếp: **ADR-033***

---

## Bảo trì

File này **viết tay** — không phải vùng generated.

- **Thêm ADR:** lấy **mã kế tiếp** ghi ở dòng trên (không tái sử dụng mã đã cấp, kể cả khi ADR bị gộp/thay thế), viết theo khuôn [TEMPLATE.md](TEMPLATE.md) thành `ADR-NNN-yyyy-MM-dd-slug.md`, thêm **một dòng** vào bảng + tăng "mã kế tiếp" trong **cùng commit**.
- **Đổi trạng thái:** sửa cột Trạng thái + chuyển dòng cho đúng thứ tự ưu tiên. **Không đổi tên file, không đổi mã** — đó là lý do trạng thái không nằm trong tên.
- **"Gộp / bị thay thế" không phải trạng thái** mà là ghi chú: ADR đó khép hồ sơ (🟢 Done) và ghi rõ trỏ sang mã nào; việc còn lại theo dõi ở ADR đích.
- **🟣 Cancel** dùng khi việc **bị bỏ, không làm nữa** (đổi định hướng, công cụ rời lộ trình). Không xoá file — ghi rõ ADR nào thay và phần nào được kế thừa.
- **Tham chiếu chéo:** trong ADR và trong hội thoại, gọi bằng mã (`ADR-014`) thay vì tên file.
- Mã `ADR-NNN` ở đây là **quyết định định hướng của repo `ai-skills`**; đừng nhầm với `ADR-NNN` trong `docs/` của **dự án đích** (hai không gian tên tách biệt).
- **Không** chép nội dung ADR vào đây — chi tiết quyết định, trade-off và mục **Ảnh hưởng** nằm trong từng ADR.
- Liên quan: [AGENTS.md](../AGENTS.md) (quy ước ADR) · [COORDINATION.md](../contracts/README.md) (handoff H1–H6) · [minipower/SKILL.md](../sdlc/SKILL.md) (router).
