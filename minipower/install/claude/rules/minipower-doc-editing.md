---
paths:
  - "docs/**/*.md"
---

# Sửa DOC

- Giữ header Draft (`Version = —`) khi chưa sign-off — theo doc-versioning.md.
- Cross-ref bằng ID (`MGC-FR-012`), không copy nội dung FR từ module khác.
- Test strategy: thêm **dòng bảng**, không viết lại intro/scope.
